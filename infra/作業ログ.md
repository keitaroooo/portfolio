# インフラ作業ログ

Terraform まわりの**作業メモ**。**リソースの正は `*.tf`**（必要なら state）。このファイルは手順・前提の整理用。

## 方針

- **デプロイ設定**: このリポジトリ（`infra/terraform/cloudflare/`）で管理。
- **DNS 管理**: [`keitaro-yamaguchi`](https://github.com/keitaroooo/keitaro-yamaguchi) リポジトリに集約。

---

## Cloudflare Pages

コード: [`infra/terraform/cloudflare/`](terraform/cloudflare/)

| リソース | 説明 |
| --- | --- |
| `cloudflare_pages_project.portfolio` | Cloudflare Pages プロジェクト（静的サイト, root: `src`） |
| `cloudflare_pages_domain.www` | カスタムドメイン `www.keitaroooo.com` |

### 操作手順

```fish
source infra/load_tokens.fish
cd infra/terraform/cloudflare
terraform init && terraform plan && terraform apply
```

import ブロックは `main.tf` に記載済み。既に state に取り込まれたリソースは no-op になる。
